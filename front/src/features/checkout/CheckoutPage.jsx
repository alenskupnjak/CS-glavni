import React from 'react';
import { Typography } from '@mui/material';

function CheckoutPage() {
	return (
		<React.Fragment>
			<Typography variant="h3">Only logged in users should be able to see</Typography>
		</React.Fragment>
	);
}

export default CheckoutPage;
